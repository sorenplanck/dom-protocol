# D-015 — Bitcoin evidence module (Annex M M.9, M.16)

```text
D-015  DATE TO BE SET  RATIFICATION PENDING
  Problem:       F5 needs a trust-minimized, non-custodial Bitcoin
                 evidence module that verifies headers, txid Merkle
                 inclusion, witness shape and the key-path/script-path
                 outcome, and feeds only a verified PUBLIC outcome into
                 the USPE (M.9). It must never sign, fund, select a
                 nonce, adapt a signature, hold custody or see `t`.
  Decision:      adopt `crates/adapters/btc-evidence` as the native
                 evidence module for F5 (verification over the audited
                 `bitcoin` crate; no primitive reimplemented, M.0.4).
                 The operator-owned Keystone (BUSL-1.1,
                 keystone-settlement/keystone) is the ratifiable
                 ALTERNATIVE: pin it by exact git revision behind the
                 same M.9 boundary, its from-scratch verifier and real
                 mainnet-block fixtures re-run inside our harness
                 (M.15.2 — prior Bitcoin-Core results inform but do not
                 count for the gate). Both modules are interchangeable
                 (M.9.1 "replaceable"); neither ever signs or custodies.
  Rejected alt.: a custodial oracle; trusting a single provider's
                 assertion; letting Keystone sign/fund/adapt; vendoring
                 Keystone into this repo without the operator's IP grant.
  IP note:       the operator is sole owner of both this repo
                 (proprietary → MIT on the F8 DOM integration, D-010/
                 D-011) and Keystone (BUSL-1.1). A Keystone pin keeps
                 Keystone under its own licence; vendoring it would
                 require the operator to grant this project's terms over
                 that code. Recommendation: pin by git rev, do not
                 vendor.
  Evidence:      M.9.3 verifier checks, witness-shape/annex rejection,
                 Merkle inclusion, header PoW/linkage, USPE bridge that
                 carries no secret; real-block E2E fixtures at steps
                 14-16.
  Impact:        crates/adapters/btc-evidence, uspe, observer, the E2E
                 runbook.
  Status:        RATIFICATION PENDING until the authority signs.
```

The executor prepares this record; it does not sign on the authority's
behalf and does not self-ratify (M.16.1).
