[package]
name = "f2-model"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true  # MIT by being DOM (absorption licence-by-inheritance)
publish = false  # D-ORQ-9: explicit, never inherited

# Model checker for the F2 properties (spec §17). It checks the REAL
# machine (`kaystra_core::state::transition`) against an abstract world;
# it is an adjudication tool, never a production path.
[dependencies]
kaystra-core = { path = "../kaystra-core" }
