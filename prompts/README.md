# Prompt Files

Development prompts live under `prompts/*.txt`.

`dom-agent-runner.exe` can list available prompts and run them directly.

Each run copies the prompt text into the run report directory so the exact instructions are auditable later.
