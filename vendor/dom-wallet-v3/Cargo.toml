[workspace]
resolver = "2"
members = [
    "crates/dom-wallet-domain",
    "crates/dom-wallet-crypto",
    "crates/dom-wallet-storage",
    "crates/dom-wallet-core",
    "crates/dom-wallet-embedded-core",
    "crates/dom-wallet-core-sync",
    "crates/dom-wallet-core-submit",
    "crates/dom-wallet-core-protocol",
    "crates/dom-wallet-core-recovery",
    "crates/dom-wallet-core-restore",
    "crates/dom-wallet-remote-source",
    "crates/dom-wallet-updater",
    "crates/dom-wallet-node-manager",
    "crates/dom-wallet-production-backend",
    "src-tauri",
]

[workspace.package]
version = "0.3.2"
edition = "2021"
authors = ["Soren Planck <sorenplanck@tutamail.com>"]
repository = "https://github.com/sorenplanck/dom-wallet-v3"
license = "MIT"

[workspace.dependencies]
argon2 = "0.5"
base64 = "0.22"
bip39 = "2.1"
chacha20poly1305 = "0.10"
fs2 = "0.4"
futures-util = "0.3"
hex = "0.4"
hkdf = "0.12"
minisign-verify = "0.2.5"
rand = "0.8"
randomx-rs = "1.4.1"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls", "stream"] }
reqwest13 = { package = "reqwest", version = "=0.13.4", default-features = false }
semver = { version = "1", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
tempfile = "3"
thiserror = "2"
time = { version = "0.3", features = ["formatting", "parsing", "serde"] }
tokio = { version = "1", features = ["rt", "sync", "time"] }
tracing = "0.1"
tracing-subscriber = "0.3"
url = { version = "2", features = ["serde"] }
uuid = { version = "1", features = ["v4", "serde"] }
zeroize = { version = "1", features = ["derive"] }
tauri = { version = "2.11.2", default-features = false, features = ["wry"] }
tauri-build = "2.6.2"
tauri-plugin-updater = "=2.10.1"

# The transaction engine uses the immutable DOM protocol implementation as the
# sole authority for slate construction, transaction serialization, proof and
# signature validation.
#
# Stage 13: these were git dependencies pinned at rev
# 6f8a947dee0e54c3421caa295755b1746c178137, redirected to the audit
# workspace's crates by a root [patch] section. The shared-output BP guard
# forbids any [patch] override (it could redirect the backend), so the same
# identity is now stated directly: relative paths into the workspace that
# vendored this wallet. One type universe, no indirection. The rev stays in
# this comment as provenance.
dom-core = { path = "../../crates/dom-core" }
dom-crypto = { path = "../../crates/dom-crypto" }
dom-consensus = { path = "../../crates/dom-consensus" }
dom-serialization = { path = "../../crates/dom-serialization" }
dom-tx = { path = "../../crates/dom-tx" }
dom-slate = { path = "../../crates/dom-slate" }
dom-config = { path = "../../crates/dom-config" }
dom-chain = { path = "../../crates/dom-chain" }
dom-node = { path = "../../crates/dom-node" }
dom-pow = { path = "../../crates/dom-pow" }
dom-wallet = { path = "../../crates/dom-wallet" }
dom-wallet-core-api = { path = "../../crates/dom-wallet-core-api" }
dom-wallet-keys = { path = "../../crates/dom-wallet-keys" }
dom-wallet-recovery = { path = "../../crates/dom-wallet-recovery" }
dom-adaptor = { path = "../../crates/dom-adaptor" }

# Sidecar release authentication and atomic pointer handling are deliberately
# consumed from their reviewed protocol commit. This does not move the wallet's
# consensus crates away from the immutable revision above.
dom-sidecar = { git = "https://github.com/sorenplanck/dom-protocol", rev = "ab45a2944f22fe00f9b12984354f0d5d7cdd229a" }
